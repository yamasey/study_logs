オブジェクトが以下のことを知っている時、依存関係がある。
1. 他のクラスの名前
2. self以外の何処かに送ろうとするmethod名
 - HogeクラスはFooインスタンスがbarに応答することを予想している。
3. メッセージが要求する引数
 - HogeクラスはFoo.newにbarとtarが必要なことを知っている。
4. それらの引数の順番
 - 第一にbar、第二にtarである必要がある。
これらはFooクラスへの変更によってHogeクラスの変更が強制される可能性が高い。
